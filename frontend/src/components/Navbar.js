import React from 'react';

function Navbar() {
    return (
        <nav>
            <h1>Shopping Cart</h1>
            <div>
                <button>Login</button>
                <button>Register</button>
            </div>
        </nav>
    );
}

export default Navbar;
